// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import "@testing-library/jest-dom";

// import { CookieJar } from "tough-cookie";
// const cookieJar = new CookieJar();
// let getcookies = cookieJar.getCookies("a=b")
// // let newCookie = cookieJar.setCookieSync("a=b; Domain=localhost", "http://localhost:3000", { allowSpecialUseDomain: true, rejectPublicSuffixes: false });
// // cookieJar.setCookie("name=value", "http://localhost:3000/", Option:{allowSpecialUseDomain:true, rejectPublicSuffixes: false});


// import { CookieJar } from "tough-cookie";
// var cookiejar = new CookieJar();
// cookiejar.setCookie("name=value", "http://localhost:3000/", [allowSpecialUseDomain:true, ])