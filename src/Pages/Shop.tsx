import React from "react";
import Products from "../Component/Products";

const Shop = () => {
    // const setCart = (value: number) => {
    //     props.setCart(value);
    // };

    return (
        <>
            <Products />
        </>
    );
};

export default Shop;
