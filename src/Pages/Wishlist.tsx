import React from "react";

const Wishlist = () => {
    return (
        <>
            {/* {new Error("Something went wrong")} " this line is used to Forcefully throw error" */}
            <div>Wishlist</div>
        </>
    );
};

export default Wishlist;
